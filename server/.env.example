# server/.env.example
# Copy this file to .env — no edits needed for local development.
# Never commit .env — it is in .gitignore.

# PostgreSQL
POSTGRES_DB=propeliq_dev
POSTGRES_USER=propeliq
POSTGRES_PASSWORD=propeliq_dev_local

# Redis (leave empty for local dev — no auth required with redis:7-alpine defaults)
REDIS_PASSWORD=

# ASP.NET Core
ASPNETCORE_ENVIRONMENT=Development
